namespace Container_With_Most_Water;

class Program
{
    static void Main(string[] args)
    {
        int [] numbers = [1,8,6,2,5,4,8,3,7];
        Console.WriteLine(FindSquare(numbers));
    }
    static int FindSquare(int[] numbers)
    {
        int firstPointer;
        int secondPointer;
        for (int i = 0; i < numbers.Length; i++)
        {
            
        }
        return 0;
    }
}